<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.eclipse.keyple.demo.control">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KeypleControl">

        <activity
            android:name="org.eclipse.keyple.demo.control.activities.NetworkInvalidActivity"
            android:label="@string/title_activity_network_invalid"
            android:theme="@style/Theme.KeypleControl.NoActionBar"/>
        <activity
            android:name="org.eclipse.keyple.demo.control.activities.CardContentActivity"
            android:label="@string/title_activity_card_content"
            android:theme="@style/Theme.KeypleControl.NoActionBar"/>
        <activity
            android:name="org.eclipse.keyple.demo.control.activities.CardReaderActivity"
            android:label="@string/title_activity_card_reader"
            android:theme="@style/Theme.KeypleControl.NoActionBar" />
        <activity
            android:name="org.eclipse.keyple.demo.control.activities.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.KeypleControl.NoActionBar" />
        <activity
            android:name="org.eclipse.keyple.demo.control.activities.SettingsActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.KeypleControl.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>